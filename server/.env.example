# Google Places API key (optional).
# When set, Places search uses the official Places API instead of keyless scraping.
GOOGLE_PLACES_API_KEY=

# SQLite database location (defaults to <server>/data/odify.db).
# Override to persist history in a custom location.
ODIFY_DB_PATH=